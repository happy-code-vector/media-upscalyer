# Real-ESRGAN Video Upscaler - Requirements
# For Ubuntu with NVIDIA GPU (CUDA 12.1)

# ============================================
# STEP 1: Install PyTorch with CUDA support first
# pip install torch==2.2.0 torchvision==0.17.0 --index-url https://download.pytorch.org/whl/cu121
# ============================================

# Core dependencies
setuptools>=70.0.0

# Real-ESRGAN and dependencies
basicsr>=1.4.2
realesrgan>=0.3.0
facexlib>=0.2.5
gfpgan>=1.3.5

# Image/Video processing (use 4.8.1.78 for numpy<2 compatibility)
opencv-python==4.8.1.78
Pillow>=10.0.0
imageio-ffmpeg>=0.4.9

# Numerical computing (must be <2 for PyTorch 2.2.0)
numpy>=1.24.0,<2.0.0
scipy>=1.11.0

# Utilities
tqdm>=4.65.0
requests>=2.31.0
pyyaml>=6.0

# ============================================
# INSTALLATION INSTRUCTIONS (Ubuntu GPU):
# ============================================
# 1. Create virtual environment:
#    python3 -m venv env
#    source env/bin/activate
#
# 2. Install PyTorch with CUDA 12.1:
#    pip install torch==2.2.0 torchvision==0.17.0 --index-url https://download.pytorch.org/whl/cu121
#
# 3. Install other dependencies:
#    pip install -r requirements.txt
#
# 4. (Optional) Clone Real-ESRGAN if not auto-cloned:
#    git clone https://github.com/xinntao/Real-ESRGAN.git
#    cd Real-ESRGAN && pip install -e . && cd ..
#
# 5. Run the upscaler:
#    python upscale_video.py -i input.mp4 -m realesr-animevideov3
#
# ============================================
# TROUBLESHOOTING:
# ============================================
# NumPy version error ("compiled using NumPy 1.x cannot run in 2.x"):
#   pip install "numpy<2"
#
# OpenCV numpy conflict:
#   pip install opencv-python==4.8.1.78
#
# torchvision.transforms.functional_tensor error:
#   pip install --upgrade basicsr
#
# ============================================
# FOR CPU ONLY (slower):
# pip install torch==2.2.0 torchvision==0.17.0
# pip install -r requirements.txt
# python upscale_video.py -i input.mp4 --cpu
# ============================================
